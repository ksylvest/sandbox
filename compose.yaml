services:
  app:
    build: .
    command: [ "fastapi", "dev", "--host", "0.0.0.0", "--port", "8000", "main.py" ]
    develop:
      watch:
        - action: rebuild
          path: requirements.txt
        - action: sync
          path: ./
          target: /app
    ports:
      - "8000:8000"
